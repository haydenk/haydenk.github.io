#!/usr/bin/env sh
#MISE description="Enable repo-managed git hooks (.githooks/) for this clone"

set -e
git -C "$MISE_PROJECT_ROOT" config core.hooksPath .githooks
echo "✓ core.hooksPath set to .githooks for this clone"
echo "  Pre-commit will run: dagger call lint && dagger call html-test"
echo "  Bypass per-commit with: git commit --no-verify"
