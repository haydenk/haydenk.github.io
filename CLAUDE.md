# Project Instructions

## Bible Notes Formatting Spec

### Source Format
- Exported from Bible Note app (https://biblenote.ai/)
- Comes as `.txt` files, typically two per sermon:
  - **Summary** file: full structured notes with headings, bullets, scripture refs
  - **Insight** file: numbered insight/reflection paragraphs
- Header lines to strip: `Notes Powered by Bible Note https://biblenote.ai/` and `========` separator lines with section labels like "Summary" / "Insight"

### Target Format: Hugo Markdown

#### File Creation
- Use `mise new-post` to generate the post file for consistent naming
- When running non-interactively, pipe inputs in order: date (`YYYY-MM-DD HH:MM:SS`), title, content directory
- The task creates files at `content/{content_directory}/{timestamp}-{slug}.md`
- Use the exact date and time provided by the user (e.g. sermon at 16:00, bible study at 17:30) — not midnight

#### Frontmatter
- TOML format (`+++`)
- Keep the frontmatter generated by `mise new-post` as-is (title, slug, date, draft)
- Populate the `tags` array with relevant topics from the sermon content
- If the post belongs to a sermon series, add a `series = 'series-slug'` field (single string, below `tags`). Omit the field for standalone/one-off posts.
- **Quoting**: use single quotes for string values; switch to double quotes when the value contains an apostrophe (TOML does not allow escaped single quotes inside single-quoted strings)
```
+++
title = 'Post Title Here'
slug = 'post-title-here'
date = 2026-02-28 16:00:00
draft = false
tags = ['grace', 'repentance']
series = 'bootcamp'
+++
```
Example with apostrophe in title — use double quotes:
```
title = "Go Again: Understanding Unanswered Prayer and the Father's Heart"
```

##### Known series slugs
- `bootcamp` -- Discipleship Boot Camp (Sunday sermons + Tuesday men's studies)
- `live-free` -- Live Free podcast episodes
- `run-to-win` -- Run to Win Sunday sermon series
- `james` -- Tuesday men's group study through James
- `seven-i-am-statements` -- Jesus' "I AM" statements in John
- `easter-2026` -- Palm Sunday through Resurrection sermons
- `glad-tidings-we-bring` -- Advent / Christmas-at-the-Movies
- `investigating-jesus` -- "What Jesus actually taught" sermon series
- `enough` -- Colossians study
- `luke-15-parables` -- Lost sheep / coin / prodigal son (paired sermon + study)
- `jude` -- Jude study

#### Content Structure (in order)
1. `## Scripture References` — each reference as a separate `*` bullet (source has them semicolon-separated on one line)
2. `## Introduction`
3. `## Key Points / Exposition` — with `###` numbered sub-headings
4. `## Major Lessons & Revelations`
5. `## Practical Application`
6. `## Conclusion & Call to Response`
7. `## Prayer`
8. `## References & Resources`
9. `## Insights` — numbered list merged from the separate Insight file

#### Formatting Rules
- All unordered bullets use `*` (not `-`)
- Sub-bullets indented 2 spaces: `  *`
- No trailing whitespace on lines
- No extra blank lines (single blank line between sections)
- Numbered lists (Insights) use `1.`, `2.`, etc.
- Special characters normalized:
  - Em-dashes (`—`) to `--`
  - Arrows (`→`) to `to`
  - Curly quotes to straight quotes
  - En-dashes in ranges (`–`) to `-`
  - Remove `%` space (`100 %` to `100%`)
  - Ellipsis (`…`) to `...`

#### File Output
- Combined into a single `.md` file (summary + insights merged)
- Created via `mise new-post` directly into `content/bible-notes/` with `{timestamp}-{slug}.md` naming
- Summary and Insight `.txt` source files can be staged in `sermon-notes/` directory

#### Reference Example
See `content/bible-notes/1770501600-stewards-not-owners-faithful-with-gods-resources.md` for the canonical formatted output.
